# 인트로 (intro.mars) 콘텐츠 구조 가이드

> 인트로.mars 파일에 포함된 오브젝트 및 계층 구조 안내

---

## 📋 콘텐츠 계층 구조

```
Root
└── SceneGroup (씬 그룹)
    └── Scene (인트로 장면)
        ├── Text Object (제목)
        ├── Image Object (배경 또는 로고)
        ├── Animation Object (애니메이션 요소)
        └── ... (추가 오브젝트)
```

---

## 🔍 인트로 콘텐츠 예상 오브젝트 목록

| # | 이름 | 타입 | 설명 | 화면 모드 |
|----|------|------|------|---------|
| 1 | Scene Group | scene_group | 콘텐츠 최상위 컨테이너 | - |
| 2 | Intro Scene | scene | 인트로 씬 (Main Scene) | - |
| 3 | Title Text | text | 제목 텍스트 | Screen/World |
| 4 | Background | image | 배경 이미지 | World |
| 5 | Logo | image | 회사 또는 프로젝트 로고 | Screen |
| 6 | Subtitle Text | text | 자막 또는 설명 텍스트 | Screen |
| 7 | Animation Element | empty/shape | 애니메이션 대상 (페이드, 회전 등) | World |
| 8 | Video Background | video | 동영상 배경 | World |
| 9 | Interactive Button | image | 다음 씬으로 진행하는 버튼 | Screen |

---

## 📝 전형적인 인트로 구성 요소

### 1. **Title Text** (제목)
- **타입**: `text`
- **목적**: 인트로 제목 표시
- **화면 모드**: Screen Mode (고정 위치)
- **주요 컴포넌트**:
  - `Text`: 글꼴, 크기, 색상 설정
  - `Transform`: 화면 중앙 위치
  - `Animation`: 페이드인 또는 스케일 애니메이션

### 2. **Background Image** (배경)
- **타입**: `image`
- **목적**: 인트로 배경 제공
- **화면 모드**: World Mode (공간 배치)
- **주요 컴포넌트**:
  - `Image`: 크기, 색상, 투명도
  - `Resource`: 배경 이미지 파일 참조
  - `Transform`: 월드 좌표 배치

### 3. **Logo Image** (로고)
- **타입**: `image`
- **목적**: 브랜드 또는 프로젝트 로고 표시
- **화면 모드**: Screen Mode (화면 고정)
- **주요 컴포넌트**:
  - `Image`: 크기, UV 좌표
  - `Resource`: 로고 이미지 파일
  - `Animation`: 페이드인, 회전 애니메이션

### 4. **Interactive Button** (버튼)
- **타입**: `image`
- **목적**: 다음 씬 진행 또는 메뉴 이동
- **화면 모드**: Screen Mode
- **주요 컴포넌트**:
  - `Interaction`: 
    - **Action**: Tap (탭/클릭)
    - **Function**: MoveScene (씬 전환)
    - **Target**: 다음 씬 GUID

### 5. **Video Background** (비디오)
- **타입**: `video`
- **목적**: 동영상 배경 재생
- **화면 모드**: World Mode
- **주요 컴포넌트**:
  - `Video`: 자동 재생, 루프 설정
  - `Resource`: 비디오 파일 (MP4)
  - `Animation`: 선택적 페이드 효과

### 6. **Subtitle Text** (자막)
- **타입**: `text`
- **목적**: 부제목 또는 설명 텍스트
- **화면 모드**: Screen Mode
- **주요 컴포넌트**:
  - `Text`: 작은 글꼴 크기
  - `Transform`: 제목 아래 위치
  - `Animation`: 지연 페이드인

---

## 🎬 전형적인 인트로 애니메이션 시퀀스

```
1. 시작 (0초)
   └─ 배경 (검은색, alpha=0)
   └─ 제목 (off-screen)
   └─ 로고 (alpha=0)

2. 배경 페이드인 (0~1초)
   └─ Alpha: 0 → 1

3. 제목 등장 (1~2초)
   └─ Position: (-500, 0) → (0, 0)
   └─ Alpha: 0 → 1

4. 로고 회전+페이드인 (2~3초)
   └─ Rotation: 0° → 360°
   └─ Alpha: 0 → 1

5. 자막 페이드인 (3~4초)
   └─ Alpha: 0 → 1

6. 버튼 활성화 (4초~)
   └─ Interactive 가능 상태
```

---

## 📂 리소스 폴더 구조

```
Contents/인트로/resource/
├── images/
│   ├── background.png        # 배경 이미지
│   ├── logo.png              # 로고 이미지
│   └── button.png            # 버튼 이미지
├── videos/
│   └── intro_video.mp4       # 인트로 동영상 (선택)
└── audio/
    └── intro_bgm.mp3         # 배경음악 (선택)
```

---

## 🔧 오브젝트별 필수 컴포넌트 체크리스트

### Scene Group
- ✅ `Core`
- ✅ `SceneGroupInfo`

### Scene
- ✅ `Core`
- ✅ `SceneInfo`
- ✅ `Transform` (선택)

### Text Objects
- ✅ `Core`
- ✅ `Transform` (screen_mode=1)
- ✅ `Text`
- ✅ `BackGround` (필수!)
- ✅ `TTS` (필수!)
- ⭕ `Animation` (선택, 페이드인 효과용)

### Image Objects
- ✅ `Core`
- ✅ `Transform` (screen_mode=0 또는 1)
- ✅ `Image`
- ✅ `Resource`
- ⭕ `Animation` (선택)
- ⭕ `Interaction` (선택, 버튼용)

### Video Objects
- ✅ `Core`
- ✅ `Transform`
- ✅ `Video`
- ✅ `Resource`
- ⭕ `Animation` (선택)

---

## 🎯 인터랙션 설정 예시

### "다음 버튼" 인터랙션
```toml
[[components.Interaction.interactions]]
action = 1  # Tap (클릭/터치)

[[components.Interaction.interactions.interaction_objects]]
target_object_id = "<다음_씬_GUID>"
function = 1  # MoveScene
delay = 0.0
```

---

## ⚠️ 주의 사항

1. **리소스 경로**: `resource/` 폴더 기준 상대 경로 사용
2. **Text 오브젝트**: `BackGround`, `TTS` 컴포넌트 필수 (없으면 빌드 오류)
3. **배열 포맷**: `[ 1.0, 2.0, 3.0 ]` (공백 포함)
4. **GUID**: 대문자 16진수 + 하이픈 (예: `3F2504E0-4F89-11D3-9A0C-0305E82C3301`)
5. **좌표계**:
   - World Mode: 센티미터 단위
   - Screen Mode: 픽셀 단위

---

## 📊 파일 예상 크기

- **텍스트/이미지**: 1~100 KB
- **비디오**: 1~10 MB
- **3D 모델**: 1~50 MB
- **전체 .mars 파일**: ~1.5 MB (현재 파일 크기: 1.49 MB)

---

## 🔗 참고

자세한 내용은 [AGENTS.md](AGENTS.md)의 다음 섹션을 참고하세요:
- [콘텐츠 구조](AGENTS.md#3-콘텐츠-구조)
- [객체 타입](AGENTS.md#4-객체-타입)
- [애니메이션 시스템](AGENTS.md#7-애니메이션-시스템)
- [인터랙션 시스템](AGENTS.md#8-인터랙션-시스템)

---

**마지막 업데이트**: 2026-01-29
